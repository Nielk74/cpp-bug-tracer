{
  "skill_name": "cpp-bug-tracer",
  "evals": [
    {
      "id": 1,
      "prompt": "Why can't we paste text into the Counterparty field when we select the product Cash Flow but it works for other products? The codebase is at evals/codebase relative to the skill directory.",
      "expected_output": "The agent traces: user selects Cash Flow → ProductSelector::onSelectionChanged() → TradeForm::onProductChanged() → CounterpartyField::onProductChanged() → applyProductRules() → setReadOnly(true). It identifies the bug: setReadOnly(true) blocks paste, and explains the comment in the code about the original intent being setEnabled(false). It produces a call graph and a suggested fix.",
      "files": [],
      "assertions": [
        "The report identifies CounterpartyField::applyProductRules as the location where the field is made read-only for CashFlow",
        "The report traces the call chain starting from product selection through TradeForm::onProductChanged",
        "The report identifies that setReadOnly(true) blocks paste operations",
        "The report includes a suggested fix (e.g. use setEnabled(false) instead or only block paste for CashFlow)",
        "The report includes a call graph or execution path showing at least 3 steps in the chain"
      ]
    },
    {
      "id": 2,
      "prompt": "We're getting a validation error saying 'Notional must be greater than zero' immediately when the trade form opens, before the user has entered anything. This only happens when the form first loads with no product selected. The codebase is at evals/codebase relative to the skill directory.",
      "expected_output": "The agent finds TradeValidator::validateNotional, sees that m_product defaults to ProductType::None which is not CashFlow, so the zero notional check fires. It notes that setProduct() is never called before initial validation, and that the fix is to either call setProduct() during form init or skip validation when no product is selected.",
      "files": [],
      "assertions": [
        "The report identifies TradeValidator::validateNotional as the function where the spurious error originates",
        "The report explains that m_product defaults to ProductType::None when setProduct() has not been called",
        "The report explains that the CashFlow early-return guard does not protect against the None case",
        "The report includes at least one concrete suggested fix direction",
        "The report includes a call graph or execution path showing where validation is triggered"
      ]
    },
    {
      "id": 3,
      "prompt": "Why is the Notional Amount field visible when the user selects FX, but not when they select Cash Flow? I would expect both to hide it since neither uses a notional. The codebase is at evals/codebase relative to the skill directory.",
      "expected_output": "The agent finds TradeForm::updateFieldVisibility, which explicitly shows Notional for Equity, FixedIncome, Derivative — and FX is not in the list, so it should be hidden. But the question says FX shows the notional, implying FX was accidentally included. Actually the code shows FX is correctly excluded. The interesting finding is that this might be a new product type added later where someone forgot to add it to the exclusion list — or the user is confused about which product maps to which label.",
      "files": [],
      "assertions": [
        "The report identifies TradeForm::updateFieldVisibility as where NotionalWidget visibility is controlled",
        "The report correctly lists which product types show the notional field (Equity, FixedIncome, Derivative)",
        "The report confirms that both FX and CashFlow should be hidden per the current code",
        "The report explains the logic of the boolean showNotional variable"
      ]
    },
    {
      "id": 4,
      "prompt": "When a trade is rejected by the adapter fallback in CCounterpartyService, we're loading the wrong counterparty data instead of the one matching the code we looked up. It seems like we always get the same counterparty back regardless of which code we pass. The codebase is at evals/codebase relative to the skill directory.",
      "expected_output": "The agent traces LoadCounterpartyByCode → LoadFromAdapter and finds that GetCounterpartyById(0) is called with a hardcoded 0 instead of a lookup by strCode. This means the adapter always returns whatever counterparty has ID 0, regardless of the requested code. The fix is to look up by code (e.g. GetCounterpartyByCode(strCode)) or translate strCode to an ID before calling GetCounterpartyById.",
      "files": [],
      "assertions": [
        "The report identifies CCounterpartyService::LoadFromAdapter as the location of the bug",
        "The report identifies that GetCounterpartyById is called with hardcoded 0 instead of a value derived from strCode",
        "The report traces the call path from LoadCounterpartyByCode through the DB fallback to LoadFromAdapter",
        "The report explains that the bug causes the wrong counterparty to be returned whenever the DB path fails and the adapter is used",
        "The report includes at least one concrete suggested fix"
      ]
    },
    {
      "id": 5,
      "prompt": "We have a bug where releasing a credit reservation by its numeric reservation ID always fails with 'Reservation ID not found', even though the reservation was just created and we have the correct ID. Releasing by trade ID string works fine. The codebase is at evals/codebase relative to the skill directory.",
      "expected_output": "The agent traces ReserveCredit → GenerateReservationId (which post-increments m_nNextReservationId) → then stores in m_mapReservationIdToTradeId[m_nNextReservationId] (the already-incremented value) instead of [m_nNextReservationId - 1] (the value embedded in the returned string). So the map key is off by one from the ID returned to the caller, making ReleaseCreditReservation(long) always fail to find the reservation.",
      "files": [],
      "assertions": [
        "The report identifies CCreditCheckService::ReserveCredit as the location of the bug",
        "The report identifies that GenerateReservationId uses post-increment on m_nNextReservationId",
        "The report identifies that m_mapReservationIdToTradeId is keyed with m_nNextReservationId (post-increment value) rather than m_nNextReservationId - 1 (the ID embedded in the returned string)",
        "The report traces ReleaseCreditReservation(long) and explains why the lookup fails",
        "The report includes a suggested fix, e.g. store the reservation ID before the call to GenerateReservationId or use m_nNextReservationId - 1 as the key"
      ]
    },
    {
      "id": 6,
      "prompt": "Our trade-validated notification handlers are never being triggered when a trade passes validation. The handlers are registered for NOTIFICATION_TYPE_TRADE_VALIDATED but they never fire. Rejection handlers work fine. The codebase is at evals/codebase relative to the skill directory.",
      "expected_output": "The agent finds CNotificationService::NotifyTradeValidated and sees that when bIsValid == true, it sends NOTIFICATION_TYPE_TRADE_SUBMITTED instead of NOTIFICATION_TYPE_TRADE_VALIDATED. Handlers registered for NOTIFICATION_TYPE_TRADE_VALIDATED never match, so they never fire. The fix is to use NOTIFICATION_TYPE_TRADE_VALIDATED in the true branch.",
      "files": [],
      "assertions": [
        "The report identifies CNotificationService::NotifyTradeValidated as the location of the bug",
        "The report identifies that NOTIFICATION_TYPE_TRADE_SUBMITTED is used in the bIsValid == true branch instead of NOTIFICATION_TYPE_TRADE_VALIDATED",
        "The report explains that handlers registered for NOTIFICATION_TYPE_TRADE_VALIDATED will never be dispatched",
        "The report traces how SendNotification dispatches via DispatchToHandlers using the type as the lookup key",
        "The report includes a suggested fix"
      ]
    },
    {
      "id": 7,
      "prompt": "When a settlement completes, our trade submission listeners are firing instead of our settlement completion listeners. We have listeners registered on CTradeEventDispatcher but they're receiving OnTradeSubmitted calls instead of a settlement-specific callback. The codebase is at evals/codebase relative to the skill directory.",
      "expected_output": "The agent finds CSettlementService::DispatchSettlementEvent which creates a STradeEvent with m_nEventType = 3 for COMPLETED settlements. Then CTradeEventDispatcher::DispatchEvent dispatches event type 3 to OnTradeSubmitted (case 3). So settlement completions incorrectly trigger OnTradeSubmitted on all registered listeners. Settlement failed (4) and cancelled (5) events fall into the default case and trigger nothing. The fix is to use distinct event type values for settlement events (e.g. 10, 11, 12) that don't collide with the trade event types (1=created, 2=validated, 3=submitted), or add new cases to DispatchEvent for settlement events.",
      "files": [
        "src/services/CSettlementService.cpp",
        "src/legacy/CTradeEventDispatcher.cpp"
      ],
      "assertions": [
        "The report identifies CSettlementService::DispatchSettlementEvent as where settlement events are created",
        "The report identifies that settlement COMPLETED uses event type 3, which collides with the trade SUBMITTED handler in CTradeEventDispatcher::DispatchEvent",
        "The report reads CTradeEventDispatcher::DispatchEvent and traces case 3 to OnTradeSubmitted",
        "The report explains that settlement FAILED (4) and CANCELLED (5) events fall into the default case and are silently dropped",
        "The report includes a suggested fix involving separating settlement event type values from trade event types"
      ],
      "weakness_targeted": "multi-file tracing — requires reading both CSettlementService.cpp and CTradeEventDispatcher.cpp and connecting them"
    },
    {
      "id": 8,
      "prompt": "Our workflow metrics show that m_nTotalWorkflowsStarted is always exactly double what we expect. When we run 10 full trade workflows, the counter shows 20 instead of 10. The codebase is at evals/codebase relative to the skill directory.",
      "expected_output": "The agent finds CTradeServiceFacade::FullTradeWorkflow which increments m_nTotalWorkflowsStarted++ at line 453, then calls CreateAndValidateTrade which also increments m_nTotalWorkflowsStarted++ at line 119. Every full workflow run triggers two increments. The fix is to remove the increment from FullTradeWorkflow since CreateAndValidateTrade already counts it, or guard one of the increments.",
      "files": [
        "src/services/CTradeServiceFacade.cpp"
      ],
      "assertions": [
        "The report identifies CTradeServiceFacade::FullTradeWorkflow as where the double-counting originates",
        "The report identifies that FullTradeWorkflow increments m_nTotalWorkflowsStarted directly",
        "The report identifies that CreateAndValidateTrade (called by FullTradeWorkflow) also increments m_nTotalWorkflowsStarted",
        "The report explains that every FullTradeWorkflow call triggers two increments of the same counter",
        "The report includes a suggested fix"
      ],
      "weakness_targeted": "careful reading within one file — requires reading two function bodies in CTradeServiceFacade.cpp and noticing the counter is incremented in both the caller and callee"
    },
    {
      "id": 9,
      "prompt": "We've found a serious bug: a fully settled trade can be settled again without any error. The second call to the settlement flow succeeds and produces a second settlement record for the same trade. The codebase is at evals/codebase relative to the skill directory.",
      "expected_output": "The agent traces CTradeServiceFacade::SettleAndNotify which sets context.m_eCurrentState = Settled on its local SFullTradeContext but never calls m_pLifecycleService to update the trade's lifecycle state. CTradeLifecycleService still stores the trade as Executed. On the second settlement call, CSettlementService::ScheduleSettlement calls ValidateTradeForSettlement which calls IsTradeSettleable which calls m_pTradeLifecycleService->GetTradeState — it returns Executed, bSettleable = (state == Executed) = true, so the second settlement is allowed.",
      "files": [
        "src/services/CTradeServiceFacade.cpp",
        "src/services/CSettlementService.cpp",
        "src/services/CTradeLifecycleService.cpp"
      ],
      "assertions": [
        "The report identifies CTradeServiceFacade::SettleAndNotify as the location of the root cause",
        "The report identifies that context.m_eCurrentState = Settled is set only on the local SFullTradeContext, not in CTradeLifecycleService",
        "The report traces that CSettlementService::ValidateTradeForSettlement calls IsTradeSettleable which calls GetTradeState on the lifecycle service",
        "The report explains that CTradeLifecycleService::GetTradeState still returns Executed because the lifecycle state was never updated to Settled",
        "The report includes a suggested fix involving calling the lifecycle service to record the Settled state transition after settlement succeeds"
      ],
      "weakness_targeted": "cross-layer state — facade updates its own context but does not propagate the state change to the lifecycle service; requires tracing 5 hops across 3 files. NOTE: qwen3-coder:30b consistently fails this eval, finding surface-level 'missing duplicate check' explanations instead of the root cause. The BUG comment in CTradeServiceFacade.cpp:SettleAndNotify helps but is insufficient. This is a documented model capability ceiling for cross-layer state synthesis."
    },
    {
      "id": 10,
      "prompt": "Trades for counterparties who have exceeded their credit limit are being accepted and validated instead of rejected. The credit limit check appears to run but has no effect. The codebase is at evals/codebase relative to the skill directory.",
      "expected_output": "The agent traces CreateAndValidateTrade → ValidateTrade → ValidateTradeInternal → CheckCreditLimit. In CCreditCheckService::CheckCreditLimit, when the new exposure exceeds the credit limit, the function returns ServiceResult<bool>::Success(false) instead of Failure. Back in ValidateTradeInternal, only IsFailure() is checked — because Success(false) is not a failure, the check passes and the trade is validated regardless of the credit limit.",
      "files": [
        "src/services/CTradeServiceFacade.cpp",
        "src/services/CTradeLifecycleService.cpp",
        "src/services/CCreditCheckService.cpp"
      ],
      "assertions": [
        "The report identifies CCreditCheckService::CheckCreditLimit as the location of the bug",
        "The report identifies that CheckCreditLimit returns ServiceResult<bool>::Success(false) instead of Failure when the credit limit is exceeded",
        "The report identifies that CTradeLifecycleService::ValidateTradeInternal checks only creditResult.IsFailure() and never checks creditResult.GetValue()",
        "The report traces the call chain from CreateAndValidateTrade through ValidateTrade through ValidateTradeInternal to CheckCreditLimit",
        "The report explains that Success(false) passes the IsFailure() guard, allowing over-limit trades to be validated"
      ],
      "weakness_targeted": "ServiceResult misuse — requires understanding that IsFailure() and GetValue() are separate concerns; the bug is at the boundary between two files across 4 hops"
    },
    {
      "id": 11,
      "prompt": "After we cancel a trade, subsequent trades for the same counterparty keep hitting the credit limit even though the cancelled trade's credit reservation should have been freed. The credit is never released back. The codebase is at evals/codebase relative to the skill directory.",
      "expected_output": "The agent traces CancelTrade → ReleaseCreditForTrade in CTradeLifecycleService. ReleaseCreditForTrade calls m_pCreditCheckService->ReleaseCreditReservation(refData.m_strCreditReservationId) — passing the reservation ID string instead of the trade ID string. CCreditCheckService::ReleaseCreditReservation(const string&) looks up by trade ID in m_mapCreditReservations, so the lookup always fails (NOT_FOUND). Because the release fails, m_mapCounterpartyReservedTotals is never decremented, so the counterparty's reserved total remains inflated after cancellation.",
      "files": [
        "src/services/CTradeLifecycleService.cpp",
        "src/services/CCreditCheckService.cpp"
      ],
      "assertions": [
        "The report identifies CTradeLifecycleService::ReleaseCreditForTrade as the location of the bug",
        "The report identifies that ReleaseCreditReservation is called with refData.m_strCreditReservationId (reservation ID) instead of refData.m_strTradeId (trade ID)",
        "The report traces CCreditCheckService::ReleaseCreditReservation(const string&) and explains that it looks up by trade ID in m_mapCreditReservations, not by reservation ID",
        "The report explains that because the lookup always fails, m_mapCounterpartyReservedTotals is never decremented",
        "The report includes a suggested fix: pass refData.m_strTradeId to ReleaseCreditReservation"
      ],
      "weakness_targeted": "wrong-argument bug — a meaningful variable (reservation ID) is passed where a different meaningful variable (trade ID) is expected; requires tracing through the credit service's data structures across 4 hops"
    }
  ]
}
